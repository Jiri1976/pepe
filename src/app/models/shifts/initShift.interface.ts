export interface InitShift {
    id: number,
    date: Date,
    from: Date,
    to: Date,
    perso?: string,
    position: string,
    destination: string,
    createdAt?: string,
    createdBy?: string,
    updatedAt?: string,
    updatedBy?: string
}